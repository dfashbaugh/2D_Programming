#include <iostream>
#include "InstructionType.h"

using namespace std;

int main(void)
{
	cout << "Hi Everyone" << endl;

	/*
	// Example Functional Execution Code
	int registers [255];
	for(int i = 0; i<255; i++)
	{
		registers[i] = 0;
	}

	TwoDimensionalInstruction* instructions[10][10];
	instructions[0][0] = new AddInstruction(0, 0, 1, TwoDimensionalAddress(0,1));

	TwoDimensionalAddress myAddr = instructions[0][0]->Execute(registers);
	*/

	return 0;
}